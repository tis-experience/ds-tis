# Contrato atual — Modal

- Web: `css/components/modal.css` + `js/modal.js`.
- Visual: `tokens/component/modal.json` e anatomia `ds-modal*`.
- Behaviors: modal, focus trap, Escape, outside click, retorno de foco, scroll
  lock e controlled/uncontrolled conforme a tecnologia.
- Sizes: `sm`, `md`, `lg`; close icônico e footer com Buttons públicos.
- Figma, tokens e core Web foram preservados.
