# Current contract

Table Web é `app-ready`, usa HTML nativo e mantém a anatomia pública documentada em `docs/table.html` e `css/components/table.css`. O contrato oferece sm/md, fixed, nowrap, row selected, células alinhadas e botão de ordenação dentro do header. A saída React estava unavailable no catálogo canônico antes desta run.
