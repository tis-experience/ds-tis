# Contrato atual — Radio

- Web: input radio nativo em `fieldset`/`legend`, CSS tokenizado e sem runtime próprio.
- Figma: página `136:2`, root `137:2`, 38 Component Variables e zero issue na página.
- React: item `@tis/radio-group`, Base UI, source distribuído pelo registry.
- Ark/Zag: ausência atual identificada como gap de cobertura.
- Estados e sizes vêm de `docs/radio.html`, `css/components/radio.css` e `tokens/component/radio.json`.
