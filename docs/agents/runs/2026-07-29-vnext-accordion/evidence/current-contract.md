# Contrato atual — Accordion

- Web estável: `css/components/accordion.css` + `js/accordion.js`.
- Contrato visual: 28 tokens `component.accordion.*` e anatomia
  `ds-accordion*` documentada em `docs/accordion.html`.
- Contrato comportamental comum: single/multiple, controlled/uncontrolled,
  trigger/panel associados, item disabled, teclado roving e conteúdo livre.
- Figma, tokens e Web core foram preservados nesta rodada.

A matriz detalhada original permanece em `accordion-contract-matrix.md`.
