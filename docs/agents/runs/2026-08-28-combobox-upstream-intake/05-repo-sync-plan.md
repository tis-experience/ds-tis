# Plano repo

- Web: preservar CSS, anatomia e API; corrigir somente o clear já documentado e
  bloquear seleção de opções `aria-disabled`.
- Ark/Zag: criar adapter isolado em `packages/react/src/ark/combobox.jsx` e stories próprias.
- React: criar `registry/tis/combobox.tsx` usando apenas Base UI e registrar `@tis/combobox`.
- Docs: adicionar Combobox ao portal Astro, assets e metadados das três saídas.
- Testes: lifecycle Web, Storybook, registry consumer, browser desktop/mobile,
  teclado, Axe e bundle.
- Tokens/Figma: nenhuma alteração.
- Bloqueado: commit, push, PR e release.
