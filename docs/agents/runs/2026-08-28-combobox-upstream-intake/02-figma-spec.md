- Status: Approved unchanged

# Resultado Figma — sem mudança

- Página: Combobox.
- Fonte visual: Figma TIS, espelhado por `docs/combobox.html`,
  `css/components/combobox.css` e `tokens/component/combobox.json`.
- Anatomia preservada: Form Field, Control, Input, Leading Icon, Clear,
  Chevron, Listbox e Option.
- Properties preservadas: Label, Placeholder, Content, icons, helper, error,
  Size, State, Filled e Read-only.
- Variants preservadas: `sm`, `md`, `lg` e os estados documentados.
- Tokens/bindings: sem criação, remoção ou alteração.
- Documentação visual: sem alteração.

As diferenças entre Ark UI e Base UI pertencem apenas às implementações. Nomes
de provider, portal, collections, callbacks e data attributes não entram no
Figma. O snapshot `.figma-snapshot.json` tinha 11 horas no kickoff e o preflight
registrou `VALUE_DRIFT=0`.
