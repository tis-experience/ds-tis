# Estado atual

- `npm run agent:preflight`: branch `codex/angular`, worktree previamente suja e preservada.
- Snapshot: 2026-08-28T08:35:08.535Z; `VALUE_DRIFT=0` e página Select com `issueCount=0`.
- Figma: página `145:2`, frame raiz `146:20`, 38 Component variables `select/*`.
- Web: `<select>` nativo, sem runtime JavaScript, App-ready e distribuído por `ds-tis/css`.
- Contrato visual: sizes `sm/md/lg`, leading icon opcional, placeholder/value, chevron, Field, error, disabled e readonly documentado.
- Limite atual: Ark/Zag planejado; React/shadcn/Base UI ainda indisponível.

Nenhum arquivo Figma, token ou CSS Web precisa ser alterado para criar os
adapters. O popup customizado pertence somente às saídas React.
