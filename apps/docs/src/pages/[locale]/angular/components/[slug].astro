---
import StarlightPage from '@astrojs/starlight/components/StarlightPage.astro';

import ComponentDocumentationPage from '../../../../components/ComponentDocumentationPage.astro';

export function getStaticPaths() {
  const slugs = ['accordion', 'button', 'checkbox', 'input', 'modal', 'popover', 'radio', 'select', 'tabs', 'textarea', 'toggle'] as const;
  return ['pt-br', 'en'].flatMap((locale) =>
    slugs.map((slug) => ({
      params: { locale, slug },
      props: { locale, slug },
    })),
  );
}

const { locale, slug } = Astro.props;
const names = { accordion: 'Accordion', button: 'Button', checkbox: 'Checkbox', input: 'Input Text', modal: 'Modal', popover: 'Popover', radio: 'Radio', select: 'Select', tabs: 'Tabs', textarea: 'Textarea', toggle: 'Toggle' };
---

<StarlightPage
  frontmatter={{
    title: names[slug as keyof typeof names],
    description: locale === 'en' ? 'Native Angular output.' : 'Saída Angular nativa.',
    tableOfContents: false,
    lastUpdated: false,
  }}
>
  <ComponentDocumentationPage slug={slug} technology="angular" locale={locale} />
</StarlightPage>
