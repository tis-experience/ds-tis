/**
 * Primary entrypoint for metadata only. Import components from the dedicated,
 * tree-shakable entrypoints: `@tis/angular/button`, `/accordion`, `/input`,
 * `/textarea`, `/checkbox`, `/modal`, `/popover`, `/radio`, `/select`, `/tabs`
 * and `/toggle`.
 */
export const TIS_ANGULAR_VERSION = "0.0.0-beta.0";
