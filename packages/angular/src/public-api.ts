/**
 * Primary entrypoint for metadata only. Import components from the dedicated,
 * tree-shakable entrypoints: `@tis/angular/accordion`, `/alert`, `/avatar`, `/badge`, `/breadcrumb`, `/button`, `/card`, `/combobox`,
 * `/divider`, `/form-field`, `/input`, `/textarea`, `/checkbox`, `/menu`, `/modal`, `/pagination`, `/popover`, `/radio`, `/select`,
 * `/skeleton`, `/spinner`, `/table`, `/tabs`, `/toast`, `/toggle` and `/tooltip`.
 */
export const TIS_ANGULAR_VERSION = "0.0.0-beta.0";
