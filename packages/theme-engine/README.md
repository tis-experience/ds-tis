# @tis/theme-engine

Ponte privada da vNext para o motor de tema estável publicado hoje por
`ds-tis/theme`.

Não publicar este workspace isoladamente: a implementação ainda vive em
`js/theme/` para preservar os exports da v1 sem duplicação.
